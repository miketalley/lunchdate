<!DOCTYPE html>
<html lang="en">
  <head>
    <script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
    <script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="contain-to-grid sticky">
      <nav class="top-bar" data-topbar data-options="sticky_on: large">
      <ul class="title-area">
    <li class="name">
      <h1><%= link_to 'Lunch Date', root_path %></h1>
    </li>
  </ul>


  <!-- Right Nav Section -->
  <section class="top-bar-section">
    <% if user_signed_in? %>
      <ul class="right">
        <li class="active"></li>
        <li class="has-dropdown">
          <%= link_to "Logged in as #{current_user.username}.", edit_user_registration_path %>
        <ul class="dropdown">
          <li><%= link_to 'Edit profile', edit_user_registration_path %></li>
          <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        </ul>
      </li>
    </ul>
    <% else %>
      <ul class="right">
        <li class="active"></li>
        <li class="has-dropdown">
          Sign Up/Login
        <ul class="dropdown">
          <li><%= link_to "Sign up", new_user_registration_path %></li>
          <li><%= link_to "Login", new_user_session_path %></li>
        </ul>
      </li>
    </ul>
    <% end %>


  <!-- Left Nav Section -->
  <ul class="left show-for-large-up small-0">
    <div class="row">
      <div class="large-3 columns">
        <li class="has-form">
          <%= link_to 'All Dates', lunch_dates_path, 'data-no-turbolink' => true, class: "small button radius", id: "dont_wrap_this" %>
        </li>
        <!-- <li class="divider"></li> -->
      </div>
      <div class="large-3 columns">
        <li class="has-form">
          <%= link_to 'Create Date', new_query_path, class: "small button radius", id: "dont_wrap_this" %>
        </li>
      </div>
      <div class="large-3 columns">
        <li class="has-form">
          <%= link_to 'View My Dates', my_dates_path, class: "small button radius", id: "dont_wrap_this" %>
        </li>
      </div>
      <div class="large-3 columns">
        <li class="has-form">
          <%= link_to 'View My Accepts', my_matches_path, class: "small button radius", id: "dont_wrap_this" %>
        </li>
      </div>
    </div>
  </ul>
  </section>
  </nav>
  </nav>
  </div>
  <!-- Flash Message Code -->
    <% flash.each do |name, msg| %>
      <% if msg.is_a?(String) %>
        <%= content_tag :div, msg, :class => "flash_#{name}" %>
      <% end %>
    <% end %><br />

    <%= link_to 'Back', :back, class: 'tiny button' %>
    <%= yield %>
  </body>
</html>
